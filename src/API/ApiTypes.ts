type SystemIdMap = {
    [key: string]: number;
};

// Definir el objeto `systemIdMap` con el tipo `SystemIdMap`
export const systemIdMap: SystemIdMap = {
    "/cuentas": 1,
    "/yimeil": 2,
    "/draiv": 3,
    "/k-lendar": 4,
};